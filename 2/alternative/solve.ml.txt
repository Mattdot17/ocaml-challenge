(* ( <|> ) : 'a option -> 'a option -> 'a option *)


let ( <|> ) x y = match x,y with
    Some x, _ ->  Some x
  | None, Some y -> Some y
  | None, None -> None;;


(* or *)

let ( <|> ) = function
    Some x, _ ->  Some x
  | None, Some y -> Some y
  | None, None -> None;;